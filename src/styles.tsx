import React from "react";

const flex : React.CSSProperties = {
    display: 'flex',
}

const alignCenter : React.CSSProperties ={
    alignItems: 'center'
}

const justifyCenter : React.CSSProperties ={
    justifyContent: 'center',
}


export { flex, alignCenter, justifyCenter }